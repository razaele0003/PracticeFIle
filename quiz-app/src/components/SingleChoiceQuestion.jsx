function SingleChoiceQuestion({ question, userAnswers, revealState }) {
  // This component just renders the options, the actual click handler is passed from parent
  return null;
}

export default SingleChoiceQuestion;
